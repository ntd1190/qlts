
@model IList<ProductOverviewModel>

@using Nop.Web.Models.Catalog;
@if (Model.Count > 0)
{
    int i = 0;
    foreach (var item in Model)
    {
        if (@i < 2)
        {
            // hien 2 item dau tien
        }
        
        @Html.Partial("_ProductList", item)
        i++;
    }
}
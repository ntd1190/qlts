@model IList<ProductOverviewModel>

@using Nop.Web.Models.Catalog;
@if (Model.Count > 0)
{  <div class="section-title clearfix">

        <a href="/" title="Sản phẩm liên quan">
            <h2>
                Sản phẩm liên quan<i class="ehis-laptop menu-icon" style="color:#37c0f4"></i>
            </h2>
        </a>
    </div>
            <div class="shop clearfix shop-masonry">
                <div class="masonry-sizer"></div>
                @foreach (var product in Model)
                {
                    <div class="masonry-item">
                        @Html.Partial("_ProductList", product)
                    </div>
                }


            </div>

    @*<div id="products-feature">
        <ul class="nav nav-tabs" role="tablist">
            <li role="presentation" class="active">
                <a href="#sanPhamMoi" aria-controls="home" role="tab"
                   data-toggle="tab" title="Sản phẩm liên quan">
                     Sản phẩm liên quan
                </a>
            </li>

        </ul>
        <div class="tab-content">
            <div role="tabpanel" class="tab-pane active fade in" id="sanPhamMoi">
                <div class="product-carousel shop clearfix">
                    @foreach (var item in Model)
                    {
                       
                    }
                </div>
            </div>
        </div>
    </div>*@
}